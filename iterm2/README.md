# iterm2

## How to set

1. [General] > [Settings] > [External settings] > Select this directory
   (Or run `./run.sh`)
2. Set [Save changes] to [Automatically] (also configured by `./run.sh`).
3. Restart iTerm2.
